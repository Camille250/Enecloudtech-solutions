apiVersion: v1
kind: Service
metadata:
  name: {{ include "enecloudtech-chart.fullname" . }}
  labels:
    {{- include "enecloudtech-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enecloudtech-chart.selectorLabels" . | nindent 4 }}
