apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enecloudtech-chart.fullname" . }}-test-connection"
  labels:
    {{- include "enecloudtech-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enecloudtech-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
