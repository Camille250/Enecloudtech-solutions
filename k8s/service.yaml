apiVersion: v1
kind: Service
metadata:
  name: enecloudtech-app
  labels:
    app: enecloudtech-app
spec:
  type: ClusterIP
  selector:
    app: enecloudtech-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
